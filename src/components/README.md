# Components

This directory contains reusable UI components for the application.

## Structure

- Store each component in its own directory (e.g., `button/button.ts`).
- Include related styles and tests in the component's directory (e.g., `button/button.scss`).
