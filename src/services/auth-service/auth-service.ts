// Will be added later after the API Service release
class AuthService {
  //private currentUser: IUser | undefined = undefined;

  public loginUser(): void {
    return;
  }
}

export const authService = new AuthService();
