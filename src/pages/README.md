# Pages

This directory contains the top-level components that represent application pages/routes.

## Structure

- Each page should correspond to a route in the application.
- Page components can compose lower-level components from `src/components`.
