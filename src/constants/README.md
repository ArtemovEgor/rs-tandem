# Constants

This directory contains global constants and configuration values.

## Guidelines

- Use this for static data, configuration strings, and magic numbers.
- Group related constants into files (e.g., `api.ts`, `theme.ts`).
